Ternary operator works like in other languages:

```cpp
condition ? ifTrue : ifFalse;
```

The ternary returns the code from the selected path:

```cpp
int x = 10;
int y = 20;
int greatest = (x > y) ? x : y; // 20
```

